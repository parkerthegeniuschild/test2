import { containerStyles, wrapperStyles } from './IconButton';

export { IconButton } from './IconButton';

export const iconButtonStyles = {
  container: containerStyles,
  wrapper: wrapperStyles,
};
