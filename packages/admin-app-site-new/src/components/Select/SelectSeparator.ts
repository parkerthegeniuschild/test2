import * as Ariakit from '@ariakit/react';

import { styled } from '@/styled-system/jsx';

export const SelectSeparator = styled(Ariakit.SelectSeparator, {
  base: {
    borderColor: 'gray.100',
  },
});
