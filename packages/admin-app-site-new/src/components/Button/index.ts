import { containerStyles, wrapperStyles } from './Button';

export { Button, type ButtonProps } from './Button';

export const buttonStyles = {
  container: containerStyles,
  wrapper: wrapperStyles,
};
